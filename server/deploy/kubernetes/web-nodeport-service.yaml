apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web-nodeport
  namespace: ne
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31000
  selector:
    app: web